// Generated by CoffeeScript 1.12.6
(function() {
  var Chunk;

  Chunk = (function() {
    function Chunk() {}

    Chunk.prototype.init = function(cfg) {
      var data, e;
      this.cfg = cfg;
      this.cfg.registered = false;
      data = {
        detail: this.cfg
      };
      try {
        e = new CustomEvent(this.cfg.type, data);
      } catch (error) {}
      if (!e) {
        e = document.createEvent('CustomEvent');
        e.initCustomEvent(this.cfg.type, false, false, this.cfg);
      }
      document.dispatchEvent(e);
      if (!e.detail.registered) {
        console.log("Error registering chunk '" + this.cfg.path + "': ", e);
      } else {
        null;
      }
      return null;
    };

    return Chunk;

  })();

  return new Chunk();

}).call(this);
